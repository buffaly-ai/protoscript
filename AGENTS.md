# Agent Instructions

## Commit Policy
- Do not commit changes until the user has reviewed the work and explicitly approved committing.
- After approval, create a git commit before finishing.
- Every commit must include a clear, descriptive commit message that summarizes the completed changes.
