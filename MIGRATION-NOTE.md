# Migration Note

This repository was migrated to the buffaly-ai organization from the existing ProtoScript source tree.

The migration export excluded build outputs, binaries, local runtime artifacts, logs, packages, and private operational data. Manual-review candidates were inspected before publication.

ProtoScript depends on the Ontology project, which is migrated separately under the buffaly-ai organization.
